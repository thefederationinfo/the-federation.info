version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres

  redis:
    image: redis

  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - db
      - redis
    environment:
      # TODO force setting outside compose
      - DJANGO_SECRET_KEY=12345

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    ports:
      - 8000:80
    volumes:
      - frontendstatics:/srv/frontend:ro

volumes:
  frontendstatics:
