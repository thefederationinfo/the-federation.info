version: '3'

services:
  redis:
    image: redis

  app:
    image: a6543/the-federation_backend:latest
    depends_on:
      - redis
    env_file:
      - .env

  web:
    image: a6543/the-federation_frontend:latest
    depends_on:
      - app
    ports:
      - "127.0.0.1:8123:80"
