[tool:pytest]
testpaths = thefederation
DJANGO_SETTINGS_MODULE = config.local
# live tests hit real fediverse instances over the network;
# run them explicitly with: pytest -m live
addopts = -m "not live"
markers =
    live: integration test against live fediverse instances
env =
    TEST=1
